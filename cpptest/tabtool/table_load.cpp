//THIS FILE IS GENERATED BY tabtool, DO NOT EDIT IT!
//GENERATE TIME [2018/3/20 16:09:18]
#include <iostream>

#include "td_chapter.hpp"
game_data::td_chapter the_td_chapter;
template<> ::game_data::td_chapter const& GET_TABLE<::game_data::td_chapter>() {
	return the_td_chapter;
}
#include "td_color.hpp"
game_data::td_color the_td_color;
template<> ::game_data::td_color const& GET_TABLE<::game_data::td_color>() {
	return the_td_color;
}
#include "td_head.hpp"
game_data::td_head the_td_head;
template<> ::game_data::td_head const& GET_TABLE<::game_data::td_head>() {
	return the_td_head;
}
#include "td_maps.hpp"
game_data::td_maps the_td_maps;
template<> ::game_data::td_maps const& GET_TABLE<::game_data::td_maps>() {
	return the_td_maps;
}
#include "td_uidata.hpp"
game_data::td_uidata the_td_uidata;
template<> ::game_data::td_uidata const& GET_TABLE<::game_data::td_uidata>() {
	return the_td_uidata;
}

namespace game_data {
bool load_all_data() {
	if(!the_td_chapter.Load()) { std::cerr << "load td_chapter failed !!!" << std::endl; return false; }
	if(!the_td_color.Load()) { std::cerr << "load td_color failed !!!" << std::endl; return false; }
	if(!the_td_head.Load()) { std::cerr << "load td_head failed !!!" << std::endl; return false; }
	if(!the_td_maps.Load()) { std::cerr << "load td_maps failed !!!" << std::endl; return false; }
	if(!the_td_uidata.Load()) { std::cerr << "load td_uidata failed !!!" << std::endl; return false; }
	//relate code
	{
		for(auto p1 = the_td_head.RawMap().begin(); p1 != the_td_head.RawMap().end(); ++p1) {
			auto& v1 = (td_head_item&)p1->second;
			auto const& k1 = v1.head_id;
			for(auto p2 = the_td_color.RawMap().begin(); p2 != the_td_color.RawMap().end(); ++p2) {
				auto const& v2 = p2->second;
				auto const& k2 = v2.color_id;
				if(k1 == k2) {
					v1.__relate__td_color_items.push_back(&v2);
				}
			}
		}
	}
	return true;
}
}
